{
  "name": "thousand-separator-ts",
  "version": "1.0.0",
  "description": "It takes a number and runs toLocalString() on it",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Forest Park",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
